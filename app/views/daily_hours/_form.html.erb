 <div class="card">
   <div class="card-body"> 
      <%= form_with(model: daily_hour, local: true) do |form| %>
        <% if daily_hour.errors.any? %>
          <div class="alert alert-danger alert-dismissible show fade">
            <div class="alert-title">Atenção!!</div>
                <button class="close" data-dismiss="alert"><span>×</span></button>
                <ul>
                    <% daily_hour.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>

        <div class="form-row">
              <div class="form-group col-md-6">
                <%= form.label :shift, for:"inputshif"%>
                <%= form.text_field :shift, id:"inputshif", class:"form-control", placeholder:"Ex.: 12" %>
              </div>
              <div class="form-group col-md-6">
                <%= form.label :description, for:"inputDesc"%>
                <%= form.text_field :description, id:"inputDesc", class:"form-control" , placeholder:"Ex.: Plantão"%>
              </div>
          </div>

          <div class="card-footer">
            <div class="form-group">
              <%= form.submit class:"btn btn-primary" %>
              <%= link_to daily_hours_path, class:"btn btn-icon icon-left btn-light" do %>
                <i class="fas fa-undo-alt"></i> Voltar
              <% end %>
            </div>
          </div>

      <% end %>
 </div>
</div>